#!/bin/bash
# Install netstat if missing and display listening ports
if ! command -v netstat &> /dev/null; then
    sudo apt-get update -y &> /dev/null && sudo apt-get install -y net-tools &> /dev/null
fi
sudo netstat -lp 2>/dev/null || ss -lpn
